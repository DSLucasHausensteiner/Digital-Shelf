services:
  db:
    image: postgres:17.6
    restart: always
    environment:
      - POSTGRES_PASSWORD=secret
    ports:
      - "5433:5432"
    volumes:
      - digital_shelf:/var/lib/postgresql/data

volumes:
  digital_shelf: